import { Typography } from "@mui/material";

const SettingSubTitle = ({ title }) => {
  return (
    <Typography fontSize="20px" fontWeight={700}>
      {title}
    </Typography>
  );
};

export default SettingSubTitle;
